TSP-with-reduced-cost-matrix
============================

Tugas Kecil III Strategi Algoritma

Enter the location of SFML Library for GUI on PATH of command prompt, or else use VC++ with SFML library, then compile everything.
